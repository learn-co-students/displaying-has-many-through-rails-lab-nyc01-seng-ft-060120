<h1> Patient: <%= @patient.name %> </h1>

<h2> Patient Age: <%= @patient.age %></h2>

<h3> Schudled Appointment(s) </h3>
<% @patient.appointments.each do |app| %>
<p><%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M")%> - <%= link_to app.doctor.name, doctor_path(app.doctor_id) %></p>

<% end %>